#!/usr/bin/env bash
sudo apt update &&  sudo apt-get update && sudo apt upgrade

# git install
sudo apt install git

# add Name to git
git config --global user.name "reset54"

# add Email to git
git config --global user.email "zerg547@gmail.com"

# check our installation
git --version

